# Optional: site.config.json already uses https://notesketch.vercel.app/.
# Set your preferred public HTTPS URL here only to override that configuration.
# You can also edit site.config.json. This environment variable overrides it.
# NOTESKETCH_SITE_URL=https://notesketch.vercel.app/
