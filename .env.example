# Optional on Vercel: its production domain is detected automatically.
# Set your preferred public HTTPS URL to override that automatic detection.
# You can also edit site.config.json. This environment variable overrides it.
# NOTESKETCH_SITE_URL=https://your-domain.com/
